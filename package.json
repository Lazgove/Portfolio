{
  "name": "mon-portfolio",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build && cp about.html dist/ && cp contact.html dist/ && cp projects.html dist/",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "^0.164.0"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
